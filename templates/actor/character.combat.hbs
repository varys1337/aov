{{!--Combat Tab--}}
<section class='character tab combat {{tab.cssClass}}' data-group='primary' data-tab='combat'>

  {{!--Hit Locations--}}
  <div class="skill-grid-header">
      {{#if isSmall}}
        <div class='gear-header primBackCol hitLocHeaderSmall'>
          <div class=''>
            <span>{{localize 'AOV.hitLocation'}}</span>
            <span class="centre" data-tooltip="{{localize 'AOV.rollHitLoc'}}"><a data-action="rollHitLoc"><i class="fas fa-dice-d20"></i></a></span>
            </div>
          <div class='centre'>{{localize 'AOV.d20'}}</div>
          <div class='centre'>{{localize 'AOV.armourPointsAbbr'}}</div>
          <div class='centre'style="grid-column: 4 / span3;">{{localize 'AOV.hpAbbr'}}</div>
        </div>
      {{else}}
        <div class='gear-header primBackCol skill-grid'>
          <div class='centre' style="grid-column: 1 / span 3;">{{localize 'AOV.hitLocations'}}</div>
          <div class="centre" data-tooltip="{{localize 'AOV.rollHitLoc'}}">
            <a data-action="rollHitLoc"><i class="fas fa-dice-d20"></i></a>
          </div>
        </div>
      {{/if}}
      <div class='gear-header {{#if singleColour}}primBackCol{{else}}secBackCol{{/if}} wounds-grid'>
        <div class='new-row left'>{{localize 'TYPES.Item.wound'}}</div>
        <div class='centre'>{{localize 'AOV.damage'}}</div>
        <div class='centre'>{{localize 'AOV.status'}}</div>
        <div class='centre'>
          {{#if @root.editable}}
          <a class='item-control item-create' data-tooltip='{{localize "AOV.createDoc" type=(localize "TYPES.Item.wound")}}'
            data-action='createDoc' data-document-class='Item' data-type='wound'>
            <i class='fas fa-plus'></i>
          </a>
          {{/if}}
        </div>
      </div>

    {{#unless isDevelop}}
      {{#if isSmall}}
        <ol class="charHitLoc">
          <div class="hitLocDev">
            {{#each devHitLocs as |item id|}}
              {{#unless (eq item.system.locType 'general')}}
                <li class="item hitLocGridSmall" data-item-id='{{item._id}}' data-document-class='Item' data-drag='true'>
                  <div class="rollable" data-action='viewDoc'>{{item.name}}</div>
                  <div class="addWndSmall" data-tooltip="{{localize 'AOV.addWound'}}" data-action='addWound'>
                    <div class="centre">{{item.system.label}}</div>
                    <div class="centre terIcon">{{item.system.map}}</div>
                    <div class="centre terIcon">{{item.system.currHp}}</div>
                    <div class="centre">/</div>
                    <div class="centre terIcon">{{item.system.hpMax}}</div>
                  </div>
                  {{#unless ../isLocked}}
                  <div class='gear-controls centre'>
                    <a class='item-delete centre' data-tooltip='{{localize "AOV.deleteDoc" type=(localize "TYPES.Item.hitloc")}}'
                      data-action="deleteDoc">
                      <i class='fas fa-trash'></i>
                    </a>
                  </div>
                  {{/unless}}
                </li>
              {{/unless}}
            {{/each}}
          </div>
        </ol>
      {{else}}
        <ol class="charHitLoc">
          <div class="hitLocGrid">
            {{#each hitLocs as |item id|}}
              {{#unless (eq item.system.locType 'general')}}
                {{#if (eq item.name 'blank')}}
                  <div></div>
                {{else}}
                  <li class="item hitlocCell" data-item-id='{{item._id}}' data-document-class='Item' data-drag='true'>
                    <div class="upper rollable" data-action='viewDoc'>{{item.name}}</div>
                    {{#if ../isLocked}}
                      <div>{{item.system.label}}</div>
                    {{else}}
                      <div class='gear-controls centre'>
                        <a class='item-delete centre' ='{{localize "AOV.deleteDoc" type=(localize "TYPES.Item.hitloc")}}'
                          data-action="deleteDoc">
                          <i class='fas fa-trash'></i>
                        </a>
                      </div>
                    {{/if}}
                    <div class="hitLocCellAtt" data-tooltip="{{localize 'AOV.addWound'}}" data-action='addWound'>
                      <div>{{localize 'AOV.armourPointsAbbr'}}</div>
                      <div class="centre terIcon"></div>
                      <div class="centre terIcon"></div>
                      <div class="centre terIcon">{{item.system.map}}</div>
                      <div class="new-row">{{localize 'AOV.hpAbbr'}}</div>
                      <div class="terIcon">{{item.system.currHp}}</div>
                      <div>/</div>
                      <div class="terIcon">{{item.system.hpMax}}</div>
                    </div>
                  </li>
                {{/if}}
              {{/unless}}
            {{/each}}
          </div>
        </ol>
      {{/if}}
    {{else}}
        <ol class="charHitLoc">
          <div class="hitLocDev">
            {{#each devHitLocs as |item id|}}
              {{#unless (eq item.name 'blank')}}
                <li class="item hitLocGridDev" data-item-id='{{item._id}}' data-document-class='Item' data-drag='true'>
                  <div class="upper rollable" data-action='viewDoc'>{{item.name}}</div>
                  <div>{{item.system.label}}</div>
                  <div class='gear-controls centre'>
                    <a class='item-delete centre' data-tooltip='{{localize "AOV.deleteDoc" type=(localize "TYPES.Item.hitloc")}}'
                      data-action="deleteDoc">
                      <i class='fas fa-trash'></i>
                    </a>
                  </div>
                </li>
              {{/unless}}
            {{/each}}
          </div>
        </ol>
    {{/unless}}

    {{!-- Wounds--}}
    <ol class="charHitLoc">
      <div class="woundsGrid">

        {{#each wounds as |item id|}}
        <li class="item wounds-grid" data-item-id='{{item._id}}' data-document-class='Item' data-drag='true'>
          <div class="rollable" data-action='viewDoc'>{{item.system.label}}</div>
          <div class="centre skill-inline"><input class="ability-cell centre" name='system.damage'
            type='number' value='{{item.system.damage}}' data-field="damage"/>
          </div>
          <div class="centre">
            {{#unless item.system.treated}}
              <a class="secIcon" data-tooltip="{{localize 'AOV.untreated'}}" data-action="itemToggle" data-property="treated">
                <i class="fa-solid fa-droplet"></i>
              </a>
            {{/unless}}
          </div>
            <div class='gear-controls centre'>
              <a class='item-delete centre' data-tooltip='{{localize "AOV.deleteDoc" type=(localize "TYPES.Item.wound")}}'
                data-action="deleteDoc">
                <i class='fas fa-trash'></i>
              </a>
            </div>

        </li>
        {{/each}}
      </div>
    </ol>
  </div>
  <br>

  {{!--Weapons--}}
  <ol class="charHitLoc">
    <div class="gear-header {{#if singleColour}}primBackCol{{else}}terBackCol{{/if}} weapons-list-grid">
      <div class='new-row left'>{{localize 'TYPES.Item.weapon'}}</div>
      <div class='centre'>%</div>
      <div class='centre' data-tooltip="{{localize 'AOV.specDamageShow'}}">{{localize 'AOV.damage'}}</div>
      <div class='centre'>{{localize 'AOV.dmgBonusAbbr'}}</div>
      <div class='centre'>{{localize 'AOV.encumbranceAbbr'}}</div>
      <div class='centre' style="grid-column: 6 / 9;">{{localize 'AOV.hpAbbr'}}</div>
      <div class='centre' data-tooltip="{{localize 'AOV.equippedHint'}}">{{localize 'AOV.equippedAbbr'}}</div>
      <div class='centre'>{{localize 'AOV.range'}}</div>
      <div class='centre'>
        <a class='centre rollable' data-tooltip='{{localize "AOV.dodge"}}'
              data-action="dodge">
              <i class='fas fa-person-running'></i>
            </a>
      </div>
    </div>

    {{#each weapons as |item id|}}
      <li class="item weapons-list-grid" data-item-id='{{item._id}}' data-drag="true" data-document-class='Item'>
        <div class="new-row gear-name left rollable truncate" data-tooltip='{{localize "AOV.viewDoc" type=(localize "TYPES.Item.weapon")}}'
              data-action='viewDoc'>{{item.name}}</div>
        <div class='gear-name centre terIcon rollable' data-action="diceroll" data-property="combat" data-skill-id="{{item._id}}" data-tooltip="AOV.rolls.combatRoll">{{item.system.total}}</div>
        <div class='gear-name centre rollable' data-action="diceroll" data-property="damage" data-skill-id="{{item._id}}" data-tooltip="AOV.rolls.damageRoll">{{item.system.damage}}{{#if item.system.special}}*{{/if}}</div>
        <div class='gear-name centre terIcon' data-tooltip="{{item.system.damTypeLabel}}">{{item.system.dbLabel}}</div>
        <div class='gear-name centre terIcon'>{{item.system.actlEnc}}</div>
        <div class="centre skill-inline"><input class="ability-cell centre" name='system.currHP'
          type='number' value='{{item.system.currHP}}' data-field="currHP"/>
        </div>
        <div class='gear-name centre'>/</div>
        <div class='gear-name centre'>{{item.system.maxHP}}</div>
        <div class='gear-name centre' data-action="itemToggle" data-property="equipStatus"><a class="">
          {{#if (eq item.system.equipStatus 1)}}
            <i class="fas fa-hand-holding" data-tooltip='{{localize "AOV.carried"}}'></i>
          {{else}}
            {{#if (eq item.system.equipStatus 2)}}
              <i class="fas fa-horse" data-tooltip='{{localize "AOV.packed"}}'></i>
            {{else}}
            <i class="fas fa-house" data-tooltip='{{localize "AOV.stored"}}'></i>
            {{/if}}
          {{/if}}
        </a></div>
        {{#if (eq item.system.weaponType 'missile')}}
          <div class='gear-name centre'>{{item.system.range}}</div>
        {{else}}
          <div class="centre">-</div>
        {{/if}}
        {{#unless ../isLocked}}
          <div class='gear-controls centre'>
            <a class='item-delete centre' data-tooltip='{{localize "AOV.deleteDoc" type=(localize "TYPES.Item.weapon")}}'
              data-action="deleteDoc">
              <i class='fas fa-trash'></i>
            </a>
          </div>
        {{/unless}}
      </li>
    {{/each}}
    </div>
  </ol>


</section>
